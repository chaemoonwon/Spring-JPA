spring:
#  datasource:
#    url: jdbc:h2:mem:test;  #메모리에서 작동하게 하는 url => Test 쿨래스를 실행할 외부 연결 없이도 실행 가능하게 함
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true  #sql
#        format_sql: true
# 스프링 부트는 별도의 설정이 없어도 메모리 모드로 실행하고 동작함.

logging:
  level:
    org.hibernate.sql: debug #하이버네이트가 생성하는 SQL문이 보임
    org.hibernate.orm.jdbc.bind: trace #쿼리 파라미터 로그 남기기
